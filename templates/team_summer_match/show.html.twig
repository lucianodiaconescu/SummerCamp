{% extends 'base.html.twig' %}

{% block title %}TeamSummerMatch{% endblock %}

{% block body %}
    <h1>TeamSummerMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Teams</th>
                <th>Match</th>
            </tr>
            <tr>
                <td>{{ team_summer_match.id }}</td>
                <td>{{ team_summer_match.team }}</td>
                <td>{{ team_summer_match.match }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('app_team_summer_match_index') }}" class="btn btn-custom">back to list</a>
    <br><br>
    <a href="{{ path('app_team_summer_match_edit', {'id': team_summer_match.id}) }}" class="btn btn-custom">edit</a>
    <br><br>
    {{ include('team_summer_match/_delete_form.html.twig') }}
{% endblock %}
