{% extends 'base.html.twig' %}

{% block title %}TeamSummerMatch{% endblock %}

{% block body %}
    <h1>TeamSummerMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_summer_match.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_team_summer_match_index') }}">back to list</a>

    <a href="{{ path('app_team_summer_match_edit', {'id': team_summer_match.id}) }}">edit</a>

    {{ include('team_summer_match/_delete_form.html.twig') }}
{% endblock %}
